diff --git a/chrome/common/chrome_paths.cc b/chrome/common/chrome_paths.cc
index ec09803ffabcf..87566a1e8e5dc 100644
--- a/chrome/common/chrome_paths.cc
+++ b/chrome/common/chrome_paths.cc
@@ -404,11 +404,13 @@ bool PathProvider(int key, base::FilePath* result) {
 
 #if BUILDFLAG(ENABLE_WIDEVINE)
     case chrome::DIR_BUNDLED_WIDEVINE_CDM:
-      if (!GetComponentDirectory(&cur)) {
-        return false;
-      }
-      cur = cur.AppendASCII(kWidevineCdmBaseDirectory);
-      break;
+       base::FilePath widevine_path = base::FilePath(FILE_PATH_LITERAL("/opt/widevine/libwidevinecdm.so"))
+       if (base::PathExists(widevine_path)) {
+         cur = widevine_path;
+         break;
+       } else {
+         return false;
+       }
 
     case chrome::DIR_COMPONENT_UPDATED_WIDEVINE_CDM: {
       int components_dir =
